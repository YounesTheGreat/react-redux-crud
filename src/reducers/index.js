export const userReducer = (state, action) => {
    switch (action.type) {
        case "ADD_USER": 
            break;
        case "REMOVE_USER":
            break;
        default:
            return state;
    }
};

export const secondReducer = (state, action) => {
    return state;
};

